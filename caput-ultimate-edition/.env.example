# ═══════════════════════════════════════════════════════════════════════════
# GENESIS 2.0 — Environment Variables
# ═══════════════════════════════════════════════════════════════════════════
# Copy to .env and fill in your values:  cp .env.example .env

# ── Required ──────────────────────────────────────────────────────────────

# JWT signing secret (min 32 chars, generate with: openssl rand -hex 32)
GENESIS_JWT_SECRET=

# ── Directories (defaults shown) ─────────────────────────────────────────

GENESIS_KEY_DIR=./data/keys
GENESIS_AUDIT_DIR=./data/audit
GENESIS_EVIDENCE_DIR=./data/evidence
GENESIS_STATIC_DIR=./static

# ── Server ────────────────────────────────────────────────────────────────

GENESIS_PDP_PORT=8080
GENESIS_OWNER_ID=owner
GENESIS_JURISDICTION=general

# ── AI Provider Configuration ─────────────────────────────────────────────

# Provider: openai, anthropic, ollama, offline, auto (auto-detect)
AI_PROVIDER=auto

# OpenAI (GPT-4)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o
# OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic (Claude)
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Ollama (Local LLM)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama2

# Legacy compatibility
# GENESIS_AI_API_KEY=sk-...
# GENESIS_AI_BASE_URL=https://api.openai.com/v1

# ── Sovereign Suite (optional — document automation) ─────────────────────

# VAULT_ROOT=~/Library/Mobile Documents/com~apple~CloudDocs/Vault
# GENESIS_CASE_NUMBER=
# GENESIS_CASE_FOLDER=Family-Case
# GENESIS_BUSINESS_NAME=
# GENESIS_TRUST_NAME=
# ANTHROPIC_API_KEY=

# ── Database (optional — enables persistent evidence storage) ────────────

# GENESIS_DATABASE_URL=postgres://genesis:password@localhost:5432/genesis

# ── FleetDM (optional — enables device management) ──────────────────────

# GENESIS_FLEET_API_URL=https://fleet.example.com
# GENESIS_FLEET_API_KEY=

# ── YubiKey (optional — hardware MFA) ─────────────────────────────────────

# Mode: "otp" (simplest), "webauthn" (browser), "challenge-response" (CLI)
# GENESIS_YUBIKEY_MODE=otp

# For WebAuthn mode (browser-based)
# GENESIS_YUBIKEY_RP_ID=localhost

# For OTP mode (Yubico cloud validation) — get from https://upgrade.yubico.com/getapikey/
# GENESIS_YUBIKEY_CLIENT_ID=12345
# GENESIS_YUBIKEY_SECRET_KEY=base64secret=

# For challenge-response mode (program into YubiKey slot 2 with ykpersonalize)
# GENESIS_YUBIKEY_HMAC_SECRET=hex-encoded-20-byte-secret

# ── Netgear Router Integration ────────────────────────────────────────────

# Router connection
NETGEAR_HOST=192.168.1.1
NETGEAR_PORT=80
NETGEAR_USERNAME=admin
# NETGEAR_PASSWORD=your-router-password
# NETGEAR_MODEL=RAX120

# ── Health Daemon ─────────────────────────────────────────────────────────

DAEMON_INTERVAL=30000
DAEMON_ALERTS=true
DAEMON_LOG=true
# DAEMON_METRICS_PORT=9090

# ── Charter Enforcement ───────────────────────────────────────────────────

CHARTER_DIR=../forbidden-ninja-city-charter-v1.0.0/charter
CHARTER_ENFORCE=true

# ── GENESIS 2.0 Control System ───────────────────────────────────────────

# Dashboard UI port
GENESIS_UI_PORT=3000

# TZOFEH Sentinel watch level: passive, active, alert, combat, sentinel
GENESIS_WATCH_LEVEL=active

# MERKAVA pulse interval (ms) — how often health checks run
GENESIS_PULSE_INTERVAL=10000

# Enable distributed tracing via MALAKH
GENESIS_TRACING=true

# ── Rust boundary (optional — firmware validation) ───────────────────────

# GENESIS_RUST_BIN=./rust_boundary/target/release/genesis_boundary
